---
roles:
  # New Relic
  - newrelic.newrelic_java_agent
  - newrelic.newrelic-infra

collections:
  # AWS
  - name: community.aws
    # version: 3.5.0
  - name: amazon.aws
    # version: 3.5.0
  
  - name: azure.azcollection
  - name: msrestazure

  # GCP
  - google.cloud

  # Windows
  - ansible.windows
  
  # MySQL
  - community.mysql
